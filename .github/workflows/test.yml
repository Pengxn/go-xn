name: test

on:
  push:
    paths-ignore:
      - 'test/azure-pipelines/**'
      - 'azure-pipelines.yml'
  pull_request:
    paths-ignore:
      - 'test/azure-pipelines/**'
      - 'azure-pipelines.yml'

jobs:
  test-linux:
    uses: ./.github/workflows/test.linux.yml
